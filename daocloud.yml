version: 2.0
test:
  image: daocloud/ci-php:5.5

  services:
    - mysql

  env:
    - MYSQL_USERNAME = "u6rZLMYuyIeaoOns"
    - MYSQL_PASSWORD = "p1ovrzIXk6MWbeEJ4"
    - MYSQL_INSTANCE_NAME = "Kcdryfz2Y9xuCDnL"

  install:
    - docker-php-ext-install pdo_mysql

  script:
    - phpunit DBTest

build:
  image:
    dockerfile_path: Dockerfile
    build_dir: /
    cache: true
